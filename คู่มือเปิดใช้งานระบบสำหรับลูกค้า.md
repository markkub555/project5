# วิธีเปิดใช้งานระบบ

เอกสารนี้ใช้สำหรับเปิดระบบบนเครื่องลูกค้าแบบ `Docker`

## 1. สิ่งที่ต้องติดตั้งก่อน

ติดตั้งให้ครบ 2 อย่าง

1. `Docker Desktop`
2. `Git`  
   ใช้เฉพาะกรณีรับงานผ่าน Git หรือ clone โปรเจกต์

หลังติดตั้งเสร็จ ให้เปิด `Docker Desktop` และรอจนขึ้นว่า `Docker is running`

## 2. เตรียมโฟลเดอร์โปรเจกต์

นำโฟลเดอร์โปรเจกต์นี้ไปวางไว้ในเครื่อง เช่น

```text
Desktop\project5
```

ภายในโฟลเดอร์ต้องมีไฟล์สำคัญอย่างน้อยดังนี้

- `docker-compose.yml`
- `Dockerfile`
- `.env`
- `src/`
- `database/init/01-schema.sql`

## 3. เตรียมไฟล์ตั้งค่า `.env`

ถ้ามีไฟล์ `.env` มาให้พร้อมแล้ว ใช้ไฟล์นั้นได้เลย

ถ้ายังไม่มี ให้สร้างจาก `.env.example`

ตัวอย่างบน PowerShell:

```powershell
Copy-Item .env.example .env
```

จากนั้นเปิดไฟล์ `.env` แล้วตรวจค่าให้ถูกต้องอย่างน้อย:

- `DB_HOST`
- `DB_PORT`
- `DB_NAME`
- `DB_USERNAME`
- `DB_PASSWORD`
- `SMTP_HOST`
- `SMTP_PORT`
- `SMTP_USERNAME`
- `SMTP_PASSWORD`
- `SMTP_FROM_EMAIL`

ถ้าใช้ Docker ตามชุดนี้ ค่า DB ฝั่ง Docker ปกติจะเป็น:

```env
DOCKER_DB_HOST=mysql-db
DOCKER_DB_PORT=3306
MYSQL_DATABASE=register
MYSQL_USER=shareduser
MYSQL_PASSWORD=1234
MYSQL_ROOT_PASSWORD=root
```

## 4. เปิด Terminal ในโฟลเดอร์โปรเจกต์

วิธีที่ง่ายที่สุด:

1. เข้าโฟลเดอร์โปรเจกต์
2. คลิกแถบ path
3. พิมพ์ `powershell`
4. กด Enter

หรือคลิกขวาในโฟลเดอร์แล้วเลือก `Open in Terminal`

## 5. สั่งรันระบบ

พิมพ์คำสั่งนี้:

```powershell
docker compose up -d --build
```

รอประมาณ `20-60 วินาที` ในการเปิดระบบครั้งแรก

## 6. ตรวจสอบว่ารันสำเร็จหรือไม่

พิมพ์:

```powershell
docker ps
```

ถ้ารันสำเร็จ ควรเห็น container หลักประมาณนี้

- `php-app`
- `mysql-db`
- `phpmyadmin`

## 7. เข้าใช้งานเว็บ

เปิด Chrome หรือ Edge แล้วเข้า:

เว็บหลัก:

```text
http://localhost:8080/login.php
```

phpMyAdmin:

```text
http://localhost:8081
```

## 8. การเปิดใช้งานครั้งต่อไป

ถ้าเคย build แล้ว และเพียงแค่ต้องการเปิดระบบใหม่ ใช้คำสั่ง:

```powershell
docker compose up -d
```

ถ้าต้องการปิดระบบ:

```powershell
docker compose down
```

## 9. หมายเหตุสำคัญ

1. ครั้งแรกที่เปิดระบบ ฐานข้อมูลจะถูกสร้างจากไฟล์ `database/init/01-schema.sql`
2. ถ้าลบ volume ของ Docker ข้อมูลในฐานข้อมูลจะหาย
3. ถ้าต้องใช้ฟังก์ชันส่งอีเมล เช่น `ลืมรหัสผ่าน` ต้องตั้งค่า `SMTP` ใน `.env` ให้ถูกต้อง
4. ถ้า port `8080` หรือ `8081` ถูกใช้งานอยู่แล้ว ต้องแก้ใน `docker-compose.yml`

## 10. ถ้าเปิดเว็บไม่ได้ ให้ตรวจตามนี้

1. `Docker Desktop` ต้องอยู่ในสถานะ `Running`
2. คำสั่ง `docker ps` ต้องเห็น `php-app`, `mysql-db`, `phpmyadmin`
3. ไฟล์ `.env` ต้องมีอยู่จริง
4. ต้องเปิดผ่าน:

```text
http://localhost:8080/login.php
```

ไม่ใช่ `localhost` port อื่น

## สรุปสั้นที่สุด

1. เปิด `Docker Desktop`
2. เปิด Terminal ในโฟลเดอร์โปรเจกต์
3. รัน

```powershell
docker compose up -d --build
```

4. เปิดเว็บที่

```text
http://localhost:8080/login.php
```
